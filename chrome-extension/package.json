{
  "name": "@curupira/chrome-extension",
  "version": "1.0.2",
  "description": "Curupira Chrome Extension - Bridge for MCP debugging",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "package": "npm run build && node scripts/create-zip.js"
  },
  "dependencies": {
    "@curupira/shared": "file:../shared",
    "webextension-polyfill": "^0.10.0"
  },
  "private": true,
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.23",
    "@types/chrome": "^0.0.260",
    "@types/webextension-polyfill": "^0.10.7",
    "@vitejs/plugin-react": "^4.2.1",
    "archiver": "^6.0.1",
    "typescript": "^5.3.0",
    "vite": "^5.0.12"
  }
}
